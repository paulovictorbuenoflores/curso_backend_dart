//Oque que tem na infra?
/*
Tudo relacionado a infraestrutura do nosso projeto
 - Utilitarios
 - Algumas classes customizadas
 - Primeira classe a ser criada é a custon service
*/

//custon middleware
na classe de middlewareInterception.dart vamos deixar de forma generica a resposta do endpoint da api de blog: headers: {'content-type': 'application/json'}
Oque é um middleware? middleware é uma classe que intercepta nossa requisicao ou response e faz coisa antes que isso seja processado
